name: kotc_core
description: A new Flutter project.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  retrofit: ^3.0.1
  json_annotation: ^4.4.0
  firebase_core: any
  firebase_auth: any
  shared_preferences: any
  freezed_annotation: ^1.1.0
  hive: ^2.0.6
  hive_flutter: ^1.1.0
  builder:
    git: https://github.com/ArishSultan/buildable.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  #  freezed: ^0.14.5
  build_runner: ^2.1.7
  json_serializable: ^6.1.4
  retrofit_generator: ^4.0.0
  hive_generator: ^1.1.2
  builder_generator:
    git: https://github.com/ArishSultan/buildable_generator.git

flutter:
